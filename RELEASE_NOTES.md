# IPChronicle v0.1.2

简体中文 | [English](RELEASE_NOTES.en.md)

本版本增加 Agent 详细日志与中心查询，改善节点管理和报告展示，并为第三方探测
请求增加有界重试。支持保留 v0.1.1 的配置、历史和节点身份升级。

## 主要变化

- Agent 记录发现、配置同步、任务和第三方请求日志，中心可按节点设置日志等级，
  按时间、等级、组件、公网 IP 和任务等条件筛选，并查看失败响应。
- 日志独立存储，默认保留 7 天；Agent 离线日志使用有界队列，恢复连接后上传。
- 第三方完整探测请求对临时错误最多尝试三次，遵循 Retry-After 和任务期限，
  记录重试与最终失败原因。
- 节点提供专属恢复安装命令，重装系统后可接管原节点并保留中心配置和历史。
- 节点列表支持多选、批量更新、批量探测和日志等级设置；公网 IP 旁可弹窗查看
  最新成功报告。
- 首页关注项聚焦需要处理的异常，NAT 路径本身不再作为异常。
- 详情页返回保留来源上下文；探测确认框突出节点名称；临时同步增加用途说明，
  列表隐藏源码摘要及内部配置计数。
- 报告明确区分“是”“否”和“无数据”，修复 PNG 字体与长标签裁切。
- 更新开发依赖的安全补丁。

## 从 v0.1.1 升级

先升级 Center，再更新 Agent。升级前停止 Center，一致备份 `./data/config` 和
`./data/history`，并保留各节点的 `/var/lib/ipchronicle-agent`。

Center 自动执行新增的日志设置和节点恢复密钥迁移，保留已有账户、主密钥、节点、
代理、计划及历史。Agent 状态 schema 保持 9，保留身份和离线结果队列。
两种 Compose 示例均新增 `./data/logs` 挂载；自定义 Compose 也应加入该挂载以在
容器重建后保留排障日志。Cloudflare Tunnel 用户继续使用对应示例并保留 token。

配置迁移后不能直接切换旧 Center 镜像回滚。需要回滚时，停止 Center，恢复升级前
的配套备份，再启动旧版本。具体操作见[运维指南](OPERATOR_GUIDE.md)。

## 待诊断问题

- 多节点共享 ipapi API Key 时部分结果缺失，尚未确认是额度、并发还是请求故障。
- 部分网络环境中 NAT 标记与实际直连地址不一致，或任务找不到已显示的公网 IP。

新增日志用于定位这些问题；本版本不宣称已修复其根因。升级后可暂时将受影响节点
设为 `debug`，复现并查看请求及任务日志，采集结束后恢复 `info`。分享日志前检查
第三方失败响应正文是否包含敏感内容。
